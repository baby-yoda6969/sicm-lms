'use client';

import React from 'react';

/** Plain background for the login form side inspired by Concord. */
export function LoginPanelDecor() {
  return (
    <div aria-hidden className="pointer-events-none absolute inset-0 bg-[#faf9f7]" />
  );
}
